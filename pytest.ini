[pytest]
DJANGO_SETTINGS_MODULE = travelhub.settings
python_files = tests.py test_*.py *_tests.py
markers =
	unit: Tests rápidos sin dependencias externas pesadas (solo lógica pura o pequeñas unidades Django)
	integration: Tests que tocan base de datos, plantillas, parsers completos o generación de PDF
addopts = -ra
filterwarnings = 
	# Suprime DeprecationWarnings externos (bindings SWIG) que no controlamos y ensucian el output
	ignore:builtin type SwigPyPacked has no __module__ attribute:DeprecationWarning
	ignore:builtin type SwigPyObject has no __module__ attribute:DeprecationWarning
	ignore:builtin type swigvarlink has no __module__ attribute:DeprecationWarning

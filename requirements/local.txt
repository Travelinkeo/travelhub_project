# Alias retrocompatible → usa dev.txt como fuente única de verdad.
# Mantener este archivo evita romper scripts o documentación que lo referencien.
# Instalar con: pip install -r requirements/dev.txt  (preferido)
-r dev.txt
